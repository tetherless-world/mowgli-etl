#import "./NodeFieldsFragment.graphql"

query NodePageQuery(
  $id: String!
) {
  nodeById(id: $id) {
    ...NodeFieldsFragment
    subjectOfEdges {
      object
      objectNode {
        id
        label
        pos
      }
      predicate
    }
  }
}
